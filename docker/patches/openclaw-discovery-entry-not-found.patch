diff --git a/src/plugins/discovery.ts b/src/plugins/discovery.ts
index 9c0ef1a..2c5d0b8 100644
--- a/src/plugins/discovery.ts
+++ b/src/plugins/discovery.ts
@@ -446,6 +446,23 @@ function resolvePackageEntrySource(params: {
   });
   if (!opened.ok) {
+    const code =
+      typeof opened.error === "object" && opened.error !== null && "code" in opened.error
+        ? String((opened.error as { code?: unknown }).code)
+        : "";
+    if (opened.reason === "path" && (code === "ENOENT" || code === "ENOTDIR")) {
+      params.diagnostics.push({
+        level: "warn",
+        message: `extension entry not found: ${params.entryPath}`,
+        source: params.sourceLabel,
+      });
+      return null;
+    }
     params.diagnostics.push({
       level: "error",
       message: `extension entry escapes package directory: ${params.entryPath}`,
       source: params.sourceLabel,
     });
     return null;
   }
